<%= render partial: "my_navbar" %>
<div class="main">
    <ul style="list-style-type: none; text-align: center; margin: 20px 0px;">
      <% @cocktails.each do |cocktail| %>
        <li> 
            <%=link_to cocktail.name, cocktail_path(cocktail) %>
        </li>
      <%end%>
    </ul>
</div>
